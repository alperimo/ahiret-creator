#version 330 core
out vec4 FragColor;
  
//in vec3 ourColor;
in vec2 texCoord;

/*uniform sampler2D texture1;
uniform sampler2D texture2;

uniform float mixRate;*/

void main()
{
    //FragColor = mix(texture(texture1, texCoord), texture(texture2, texCoord), clamp(mixRate, 0.0, 1.0)); //* vec4(ourColor, 1.0); //texture(texture1, TexCoord) * vec4(ourColor, 1.0);
    FragColor = vec4(0.8, 0.2, 0.1, 1);
}
